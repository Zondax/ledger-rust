[package]
name = "bolos-sys"
version = "0.1.0"
authors = ["Zondax <hello@zondax.ch>"]
edition = "2018"

[dependencies]
cfg-if = "1.0.0"

zeroize = { version = "1", default-features = false }

[dev-dependencies]
bolos = { version = "0.1", path = "../bolos" }
